what is a function?
->Function is a set of code that perform a specific tasks.

why use function ? 
-if sometime we need use some specific code in repetative times so that reason use function.
-code bulky(same code write mamy times so it's bulky).
-buggy(same code written many times then some mistakes in code like semicolon,colon etc).